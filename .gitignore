# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude session context (local only — never commit)
CLAUDE_PROJECT_CONTEXT.md
CLAUDE.md
.claude/

# Environment variables
.env
.env.local
.env.production
.env.development

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
backend/src/firebase-service-account.json

# Native camera-bridge binaries (built from native/camera-bridge-core via
# `npm run build:camera-bridge`; large platform artifacts, do not commit)
android/app/libs/camerabridge.aar
ios/App/Frameworks/Mobile.xcframework/

# Vendor SDK archives (download separately — exceed GitHub 100MB limit)
Monitraq/Android-SDK-*.zip
Monitraq/iOS-SDK-*.zip

# Lepu demo reference project — build outputs and local tooling
LepuDemo/app/release/
LepuDemo/.gradle/
LepuDemo/**/*.apk
LepuDemo/lepudemo.jks
LepuDemo/lepudemokey.txt

# Credentials and signing keys (never commit)
*.jks
Monitraq/staging credentials.txt
Untitled.jks
