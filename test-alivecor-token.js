const jwt = require('jsonwebtoken');

async function fetchToken() {
    try {
        // Wait, the API requires authorization via Supabase Bearer token.
        // Let's just look at the code in supabase.ts to see how it authenticates.
    } catch(e) {
        console.error(e);
    }
}
fetchToken();
